##This is the project about my personal website

###Run it using:
```
git checkout main
docker compose up
```


###Commit to remote 
```
git commit -a -m ""
git push origin main:master
```

then use browser to login http://localhost:8080/


###other notice
The img resource is in ./assets, not in _site!
Do not use WSL and remeber to open docker destop
Need to use open docker desktop first
The paper is in ./assets/pdf